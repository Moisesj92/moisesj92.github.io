GEMINI_API_KEY=
DEFAULT_TENANT=arsenio
# Postgres (Neon en Vercel). Sin esto, los mensajes se guardan en memoria (solo desarrollo).
DATABASE_URL=
# Sal para el hash de IP del rate limit
IP_HASH_SALT=
